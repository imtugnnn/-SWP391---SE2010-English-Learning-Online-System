@model List<EnglishLearningOnlineSystem.ViewModels.ContentManager.Quizzes.QuizListItemViewModel>
@{
    Layout = "_ContentManagerLayout";
    ViewData["Title"] = "Quản lý câu hỏi (Quizzes)";
    ViewData["ActiveNav"] = "quizzes";
    ViewData["Breadcrumb"] = "Câu hỏi";

    var keyword = ViewBag.Keyword as string;
    int? lessonId = ViewBag.LessonId as int?;
    int pageNumber = ViewBag.PageNumber;
    int totalPages = ViewBag.TotalPages;

    string FormatQuizType(string type) {
        return type switch {
            "MultipleChoice" => "Trắc nghiệm",
            "IMAGE_CHOICE" => "Trắc nghiệm Hình ảnh",
            "TrueFalse" => "Đúng / Sai",
            "FillInTheBlank" => "Điền vào chỗ trống",
            _ => type
        };
    }
}

<div class="cm-page-header">
    <div>
        <div class="cm-page-title">Quản lý câu hỏi</div>
        <div class="cm-page-subtitle">Tạo và quản lý các câu hỏi trắc nghiệm cho bài học</div>
    </div>
    <a asp-action="Create" asp-route-lessonId="@lessonId" class="cm-btn cm-btn-primary">+ Thêm câu hỏi</a>
</div>

@if (TempData["SuccessMessage"] != null)
{
    <div class="cm-alert" style="background:var(--cm-success-bg);color:var(--cm-success);border:1px solid #86efac;">
        @TempData["SuccessMessage"]
    </div>
}
@if (TempData["ErrorMessage"] != null)
{
    <div class="cm-alert cm-alert-danger">@TempData["ErrorMessage"]</div>
}

<div class="cm-card" style="padding:16px 20px; margin-bottom:16px;">
    <form method="get" asp-action="Index" class="row g-2 align-items-center">
        <div class="col-md-5">
            <input type="text" name="keyword" value="@keyword" class="cm-input" placeholder="Tìm theo câu hỏi..." />
        </div>
        <div class="col-md-5">
            <select name="lessonId" class="cm-select" asp-items="ViewBag.LessonSelectList">
                <option value="">-- Tất cả bài học --</option>
            </select>
        </div>
        <div class="col-md-2">
            <button type="submit" class="cm-btn cm-btn-secondary" style="width:100%;justify-content:center;">Tìm kiếm</button>
        </div>
    </form>
</div>

<div class="cm-card">
    <table class="table align-middle mb-0">
        <thead>
            <tr style="background:var(--cm-surface);">
                <th style="padding:12px 16px;">#</th>
                <th>Câu hỏi</th>
                <th>Loại</th>
                <th>Bài học</th>
                <th>Khoá học</th>
                <th class="text-end" style="padding-right:16px;">Hành động</th>
            </tr>
        </thead>
        <tbody>
            @if (!Model.Any())
            {
                <tr><td colspan="6" class="text-center py-4" style="color:var(--cm-ink-muted);">Không có câu hỏi nào.</td></tr>
            }
            else
            {
                foreach (var item in Model)
                {
                    <tr>
                        <td style="padding:12px 16px;">@item.QuizId</td>
                        <td style="font-weight:600; color:var(--cm-accent);">@item.Question</td>
                        <td><span class="cm-badge cm-badge-warning">@FormatQuizType(item.QuizType)</span></td>
                        <td>@item.LessonTitle</td>
                        <td><span class="cm-badge cm-badge-muted">@item.CourseName</span></td>
                        <td class="text-end" style="padding-right:16px;">
                            <a asp-action="Edit" asp-route-id="@item.QuizId" class="cm-btn cm-btn-ghost cm-btn-sm">Sửa</a>
                            <form asp-action="Delete" asp-route-id="@item.QuizId" method="post" class="d-inline"
                                  onsubmit="return confirm('Bạn có chắc muốn xoá câu hỏi này?');">
                                <button type="submit" class="cm-btn cm-btn-ghost cm-btn-sm" style="color:var(--cm-danger);">Xoá</button>
                            </form>
                        </td>
                    </tr>
                }
            }
        </tbody>
    </table>
</div>

@if (totalPages > 1)
{
    <nav class="mt-3">
        <ul class="pagination">
            @for (int p = 1; p <= totalPages; p++)
            {
                <li class="page-item @(p == pageNumber ? "active" : "")">
                    <a class="page-link" asp-action="Index" asp-route-page="@p"
                       asp-route-keyword="@keyword" asp-route-lessonId="@lessonId">@p</a>
                </li>
            }
        </ul>
    </nav>
}
